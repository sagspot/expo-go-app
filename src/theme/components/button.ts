const Button = {
  // Can simply pass default props to change default behaviour of components.
  baseStyle: {
    rounded: 'full',
  },
  //   defaultProps: {
  //     colorScheme: 'red',
  //   },
};

export default Button;
