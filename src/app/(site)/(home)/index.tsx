import { ScrollView, Text } from 'native-base';
import React from 'react';

const HomeScreen = () => {
  return (
    <ScrollView minH="full" bg="gray.50">
      <Text>Home screen</Text>
    </ScrollView>
  );
};

export default HomeScreen;
